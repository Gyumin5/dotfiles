[증분 자동정리 — 10분 주기] 사용자 메시지가 없어도 즉시 수행. 자동 트리거.

1) 마지막 확인시각: `cat /home/gmoh/raion/todo-sync/last_check.txt` (UTC ISO; 없으면 24h 전).
2) 메일(완전): outlook_email_search(order=newest, afterDateTime=그시각).
3) Teams DM(1:1/그룹/회의): chat_message_search(query "*", afterDateTime=그시각).
4) Teams 채널 게시글(베스트에포트): chat_message_search(query "*", limit 25, 날짜필터 없이) 호출 → 반환 항목 중 channelUri 가 있고 createdDateTime > last_check 인 것만 클라이언트측에서 골라 신규로 취급. (채널은 API 날짜필터 미지원이라 직접 시각 비교.)
4b) GitHub 알림(회사 org → 개인 Gmail ogm3614): Gmail search_threads(query "from:notifications@github.com newer_than:2d").
   · 액션 신호 = 메시지 ccRecipients 에 review_requested@ / mention@ / assign@ / team_mention@ 포함(내 리뷰요청·멘션·할당). 잡음 제외 = ci_activity@(CI), subscribed@·comment@(단순 활동/잡담), SSH키·org초대 등 정보성.
   · 각 PR/이슈는 그 스레드의 최신 메시지가 "Closed"/"Merged" 면 제외(이미 처리·불요).
   · 살아있는 액션만 후보. 제목 키 = "<repo>#<번호>"(예: raisin#148).
5) 액션 아닌 것 제외: 식대카드(식카), 단순 알림/완료통지, 광고, CI/봇 자동로그, 단순 PR잡담. 회신요청·기한·할당작업·나(오규민/gmoh) 멘션 위주만 후보.
6) 보낸편지함(folderName "Sent Items")·기존 DB(`python3 /home/gmoh/raion/todo-sync/todoctl.py json --all`) 대조해 회신완료/중복 제외. GitHub 건은 제목의 "<repo>#<번호>" 가 기존 todo 에 이미 있으면 스킵. 명확한 신규만 `todoctl.py add`(GitHub 은 source="github", 제목에 repo#번호+요약, PR 리뷰면 prio med·기한 없으면 생략).
   · Teams 회신요청/질문 후보(gmoh 지목·"규민님"·가부질문 등)는 todo 추가 전에 그 chatId 에서 chat_message_search(query "*", sender "gmoh@raionrobotics.com") 로 요청 createdDateTime 이후 gmoh 발신을 찾고, 단순히 "답했는지"가 아니라 답의 내용까지 읽어 분류한다:
     (a) 확정 해결(가부 확답·정보 제공 완료·"네 됩니다/안돼요/완료/확인했습니다" 등) → 제외, 이미 만든 todo 면 `todoctl.py done`.
     (b) 보류·조건부("생각해볼게요","나중에","검토 후 회신","이따 볼게요","일정 보고" 등 미결) → 해결 아님. 후속 todo 유지/생성(제목에 "→ 결정·회신 필요" 명시, 원 요청+gmoh 보류답 요약을 notes 에).
     (c) 부분 답 → 남은 미해결 부분만 todo.
     gmoh 발신이 아예 없으면 미회신 → 정상 후보로 처리.
   · 태그(--tags) 는 사전 정의 어휘에서만 1개 붙임: 개발(코드·PR·리뷰·기술), 회사(온보딩·공지·비품·사내행정), 인사(건강검진·보험·휴가·경비 등 HR), 회의(미팅·설명회·일정), 개인(사적). 애매하면 생략(미입력) — todoctl 이 정의 밖 단어는 자동으로 '기타'로 정리함. GitHub 건은 --tags 개발.
6b) 수신 알림 분류(B 정책): 이번 윈도우 신규 항목 중 "gmoh 직접 수신"을 가려낸다 = (1)1:1 DM 에서 타인(gmoh 아닌 발신)이 보낸 메시지, (2)채널/그룹에서 @gmoh 멘션 또는 "규민님" 등 gmoh 호명. 이들은 todo 로 안 잡혀도 한 줄 "수신 알림" 대상. 단 식카·봇알림(Workflows 등)·광고·gmoh 본인 발신은 제외. 단체방 일반 잡담(gmoh 비지목)·타인끼리 PR/멘션도 알림 대상 아님(침묵).
7) `date -u +%FT%TZ > /home/gmoh/raion/todo-sync/last_check.txt`.
8) 보고(mcp__plugin_telegram_telegram__reply, chat_id 8689118207):
   (a) 신규 todo 추가분 또는 검토 필요 후보가 있으면 보고.
   (b) 6b 의 "수신 알림" 대상이 있으면 액션이 아니어도 한 줄씩 "수신: <보낸이>(방/맥락) — <요약>" 으로 알린다. 회신 필요해 보이면 함께 표시.
   (c) (a)·(b) 둘 다 없으면 침묵(스팸 금지). 단체방 잡담·식카·봇·타인 PR잡담은 항상 침묵.
